#define USER_SCRIPT_LABEL Set Disk Read-Ahead  256
#define USER_SCRIPT_DESCR Set the "md" device read-ahead buffer size using <b>blockdev --setra 256 /dev/md?</b>
echo "<pre>"
for i in /dev/md*
do
echo Setting $i
  blockdev --setra 256 $i
  blockdev --getra $i
done
