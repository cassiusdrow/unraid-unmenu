#define USER_SCRIPT_LABEL Set Disk Scheduler to 'deadline' queuing  mode
#define USER_SCRIPT_DESCR Set the disk device I/O scheduler to use Deadline-Queuing  (deadline)
echo "<pre>"
 for i in /sys/block/[sh]d*/queue/scheduler;
 do echo -n $i " " ;
 echo deadline >$i;
 cat $i; 
done
